# Part 4 — grep verification

## 4.2 — fetch timeouts updated (no 15000 / 12000 left as abort budgets)
$ grep -rn "15000\|12000" src/
(0 hits)

$ grep -rn "controller.abort(), " src/    # the actual budgets now in place
src/App.tsx:167:    const timeoutId = setTimeout(() => controller.abort(), 25000);
src/App.tsx:391:        const timeoutId = setTimeout(() => controller.abort(), 10000);
src/App.tsx:431:    const timeoutId = setTimeout(() => controller.abort(), 10000);
src/components/HealthPill.tsx:22:      const timeoutId = setTimeout(() => controller.abort(), 8000);
src/hooks/useScanner.ts:196:    const timeoutId = setTimeout(() => controller.abort(), 20000);
src/hooks/useScanner.ts:220:    const timeoutId = setTimeout(() => controller.abort(), 20000);

## 4.3 — XAU / XAG / WTI / Commodities removed
$ grep -rn "XAU\|XAG\|WTI\|Commodities" src/
src/App.tsx:108:      // XAU/USD dropped — backend rejects gold ("Invalid pair"). BTC/USD instead.
src/components/PairSelector.tsx:14:// Commodities (XAU/XAG/WTI) intentionally absent — no backend support.
src/hooks/useScanner.ts:32:// XAU/USD removed — the backend has no gold support and always answers
src/utils/signalMeta.ts:16:export const UNSUPPORTED_PREFIXES = ['XAU', 'XAG', 'WTI', 'XPT', 'XPD'];

  Remaining hits above are intentional and are NOT pair definitions:
   - signalMeta.ts UNSUPPORTED_PREFIXES: the filter that strips a stale
     XAU/USD favourite out of a returning user's localStorage.
   - one-line comments recording why the group was removed.
  No selectable XAU/XAG/WTI pair remains in any list:
$ grep -rn "'XAU/USD'\|'XAG/USD'\|'WTI/USD'" src/
(0 hits — no selectable commodity pair)

## Part 7 — hard bans
$ grep -rn "vercel --prod\|git push\|vercel deploy" src/ scripts/
(0 hits — nothing deployed, nothing pushed)

$ git diff --stat package.json package-lock.json
(empty = no new npm dependencies, as required)

## Dead client-side auto-checker is gone (BUG #4)
$ grep -rn "const entry = due\[0\]\|checkExpired" src/
(0 hits — auto-checker removed)

$ grep -rn "api/history" src/App.tsx    # replaced by worker reconciliation
372:  // The app is a display layer now — it reconciles against `/api/history`
395:            `${API_BASE}/api/history?pair=${encodeURIComponent(cleanPair)}&limit=50`,
400:          // /api/history returns an OBJECT ({ pair, total, signals: [...] }),
